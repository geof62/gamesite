{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter une capture pour {{ project.title }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('screen_index', {'project': project.id}) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
